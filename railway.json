{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"=== Starting container ===\" && pwd && ls -la download_lfs.sh && chmod +x download_lfs.sh && ./download_lfs.sh && echo \"=== Starting server ===\" && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000} --timeout-keep-alive 600'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

