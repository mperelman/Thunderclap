# Minimal requirements for Railway deployment
# Excludes heavy dependencies that aren't needed at runtime

# Core API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# LLM API client
google-generativeai>=0.3.0

# Vector database (lightweight client)
chromadb>=0.4.0

# Text processing (minimal)
python-docx>=1.1.0
tqdm>=4.65.0
numpy>=1.24.0

# NOTE: sentence-transformers removed - models downloaded separately
# NOTE: openai removed - not used in production

