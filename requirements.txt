# Runtime requirements (minimal - excludes indexing-only packages)
# For indexing, use requirements-full.txt

# Core API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# LLM API client (only Gemini used at runtime)
google-generativeai>=0.3.0

# Vector database (lightweight client)
chromadb>=0.4.0

# Text processing (minimal)
numpy>=1.24.0

# NOTE: Removed from runtime (only needed for indexing):
# - sentence-transformers (downloads 1-2GB models, only used during indexing)
# - openai (not used in production)
# - python-docx (only used for parsing documents during indexing)
# - tqdm (only used for progress bars during indexing)


