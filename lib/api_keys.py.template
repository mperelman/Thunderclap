"""
Centralized API Keys Configuration (TEMPLATE)
==============================================
This is a template file. Copy to lib/api_keys.py and add your real keys.

ALL API keys are managed in lib/api_keys.py.
To add, remove, or update keys, edit ONLY that file.

IMPORTANT: lib/api_keys.py is in .gitignore - real keys are NOT committed.
"""

# List of all API keys for multi-key rotation
# Format: List of key strings (starting with "AIza")
# Keys marked as "REVOKED_KEY_REMOVED" are excluded automatically

API_KEYS = [
    # Add your API keys here, one per line:
    # "AIzaSy...",  # Key #1
    # "AIzaSy...",  # Key #2
    # etc.
]


def get_api_keys() -> list:
    """
    Get list of valid API keys.
    
    Returns:
        List of API key strings (filters out revoked/invalid keys)
    """
    return [
        key for key in API_KEYS
        if key and key != "REVOKED_KEY_REMOVED" and key.startswith("AIza")
    ]
